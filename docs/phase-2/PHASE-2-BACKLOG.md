# Phase 2 remainder — dependency backlog

> **Not dropped.** Per **OD-P2-2**, Phase 2 is split: `PHASE-2-CORE-PLAN.md` ships
> the MVP-blocking core; every remaining Phase 2 item from `ROADMAP.md` "Phase 2"
> is listed here with the **first consumer that forces it to land**. Each becomes a
> `phase-2r/<slug>` task (its own plan section) when its gate is reached, executed
> with the same discipline (branch-per-task, verified commit, `main` green,
> security + isolation review). None of these may be pulled forward into the core
> unless a core task proves it is genuinely required (then it is a recorded core
> plan amendment).

| #   | Deferred item                                                                                                                                                                                                                                                                                                                                                                                                   | Class | Must land **before**                                                                                                                            | Why it is safe to defer past the core                                                                                                                                                                                                                            |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1  | **`packages/money` / `packages/uom` completion pass** (split/allocation, item-specific + pack conversions, per-UOM decimal rules, fractional guards)                                                                                                                                                                                                                                                            | B     | **Phase 3** opening task (catalog + pricing)                                                                                                    | Both packages already exist and are functional; this is an extension, foldable into the Phase 3 kickoff.                                                                                                                                                         |
| B2  | **Documents domain** (`upload-intent` → quarantine → ClamAV + magic-byte + PDF/image checks → promote → signed-URL proxy; generic `(owner_type, owner_id)`; storage port)                                                                                                                                                                                                                                       | B     | **Phase 5** (supplier bills / goods-receipt attachments). _Optionally_ **Phase 3** if product media adopts it rather than a minimal image path. | No MVP-minimum domain attaches a document before procurement.                                                                                                                                                                                                    |
| B3  | **Notifications abstraction + email channel + WhatsApp share link** (`NotificationProvider` port, SMTP→Mailpit, `notification_template` / `notification_log` partitioned, `wa_share_template`, `GET /v1/whatsapp-share/link`)                                                                                                                                                                                   | B     | **Customer Web (Phase 7) production use** (order confirmations); _earlier if_ Phase 3 wants payment-receipt emails                              | OD-P2-6: on-screen confirmation is sufficient for the earliest MVP.                                                                                                                                                                                              |
| B4  | **SMS channel + SMS-OTP** (concrete vendor adapter behind the SMS port)                                                                                                                                                                                                                                                                                                                                         | C     | a release that adds **customer accounts / phone verification** (not the first Customer Web release)                                             | OD-P2-7: anonymous checkout for the MVP, no SMS-OTP dependency. Vendor pick is Z-4.                                                                                                                                                                              |
| B5  | **Registered-device foundation** (`pos_device` / `device_activation_code` / `device_policy`; `POST /v1/devices/activate` with a WebCrypto public key; device-bound session + proof; management: list / revoke / block / replace; **removes the Phase-1 amendment-1 no-op**; makes `registered_device_required` settable; enforces `max_registered_devices` / `activation_requires_mfa` / `browser_pos_allowed`) | B     | the first tenant that **requires** device-bound POS — recommended **alongside Phase 4** (POS financial control)                                 | OD-P2-5: browser POS is the MVP default; no MVP domain needs device binding. The pipeline "registered device" step stays a documented no-op until this lands.                                                                                                    |
| B6  | **Advanced device management** (DPoP proof on every request, device security dashboards, `replace` UX)                                                                                                                                                                                                                                                                                                          | C     | production hardening / a security review that asks for it                                                                                       | Layered on top of B5; not functional.                                                                                                                                                                                                                            |
| B7  | **Full audit hash chain + scheduled tamper-evidence verification job** + `security_event` alerting/correlation                                                                                                                                                                                                                                                                                                  | B→C   | **Phase 4** — built once as the shared hash-chain primitive with the **Z-Report hash chain** (OD-P2-1)                                          | Phase 2-core adds the nullable `audit_log.prev_hash` / `entry_hash` columns so this is never a migration rewrite; append-only + RLS + registry + atomic writes already hold.                                                                                     |
| B8  | **Concrete non-critical worker/scheduler jobs** (activation-code expiry, session/refresh expiry sweep beyond the core idempotency sweep, notification retry drain)                                                                                                                                                                                                                                              | B     | each job's owning feature (B5 for activation-code expiry, B3 for notification retry)                                                            | The core ships only the idempotency-key TTL sweep + outbox lag alarm.                                                                                                                                                                                            |
| B9  | **Realtime SSE fallback transport** (for restrictive networks)                                                                                                                                                                                                                                                                                                                                                  | C     | a tenant/network that blocks WebSockets in the field                                                                                            | The WS path is the MVP; SSE is a compatibility add-on with no protocol change.                                                                                                                                                                                   |
| B10 | **UI internationalization — Arabic content + RTL across all 4 web apps** (message catalogs, CLDR wiring, `translation` consumption in the UI)                                                                                                                                                                                                                                                                   | B/C   | **full GCC production / marketing launch** — _not_ the functional MVP                                                                           | OD-P2-9: English-first MVP UI is acceptable; the **data model is Arabic/RTL-ready from the core** (`translation` table, `locale.direction`, every content field has a translation path). **An English MVP is explicitly not final GCC localization compliance.** |
| B11 | **OCR purchase-invoice extraction** (`document_extraction` population)                                                                                                                                                                                                                                                                                                                                          | C     | **Phase 10**                                                                                                                                    | OD-P2-10: no OCR now; the table stays nullable / future-facing.                                                                                                                                                                                                  |
| B12 | **Analytics-warehouse CDC off the outbox**                                                                                                                                                                                                                                                                                                                                                                      | D     | volume that warrants a columnar store                                                                                                           | Architecture-ready via the outbox; not built until needed.                                                                                                                                                                                                       |
| B13 | **k6 load testing of realtime fan-out + the sale endpoint**                                                                                                                                                                                                                                                                                                                                                     | C     | **Phase 10** hardening                                                                                                                          | The core proves correctness under fault injection; load characterisation is a hardening-phase concern.                                                                                                                                                           |

## Review cadence

This backlog is re-checked **at the start of every subsequent phase**: before Phase
3, 4, 5, 6, 7 begins, confirm which B-items its "must land before" gate has reached
and schedule them as `phase-2r/*` tasks ahead of that phase's domain work. The
check is a required step in each phase plan.
