// Thin re-export barrel (task 2.5) — moved to `@flower/backend`. See
// `jwt.service.ts` in this directory for why.
export { SessionStore, SESSION_STORE, InMemorySessionStore } from '@flower/backend';
